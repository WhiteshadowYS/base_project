class Durations {
  final Duration short = Duration(milliseconds: 100);
  final Duration medium = Duration(milliseconds: 300);
  final Duration large = Duration(milliseconds: 500);

  final Duration second = Duration(seconds: 1);
}
